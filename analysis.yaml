apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: success-rate
spec:
  args:
  - name: hue-intern-service
  metrics:
  - name: success-rate
    interval: 5s
    # NOTE: prometheus queries return results in the form of a vector.
    # So it is common to access the index 0 of the returned array to obtain the value
    successCondition: data.result[0].value[1] >= 1
    # failureCondition: result[0] < 0.9
    failureLimit: 1
    provider:
      prometheus:
        address: https://monitoring.googleapis.com/v1/projects/stage-kora-dokbua/location/global/prometheus/api/v1/query
        # timeout is expressed in seconds
        timeout: 40
        headers:
        - key: 'Authorization'
          value: 'Bearer ya29.a0AXooCgulAbIjcNWwxKOZpQNV7-VpH8O1M0P3unwL9XXmsFBnN_rgeOy0KMoFQvgcshiedmElHUBfFtLXSwU8f0JZJAqb5kIHS9mU-Rg8zFwyTapTVr0gTYuys0hv4f_d15Z_NnyGM4l0YYQHD1ma0tJM5Ekm2QUh7fk4KxIKkq_raCgYKAS8SARESFQHGX2MidhKQpwiw-seirht_qS-5_Q0179'
        query: 'kubernetes_io:container_cpu_core_usage_time{container_name="hue-intern"}'
